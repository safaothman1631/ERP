{
  "_note": "Rules are NOT referenced here on purpose: helpers.js loads ../firestore.rules directly via initializeTestEnvironment (fs.readFileSync). Referencing ../firestore.rules made `emulators:exec` fail with 'outside of project directory'. Rule DEPLOYS use the repo-root firebase.json.",
  "emulators": {
    "firestore": {
      "port": 8080
    },
    "ui": {
      "enabled": false
    }
  }
}
