زۆر باشە. حازم دەبم — ئەم پلانە بنووسم بە پشت‌بەستن بە زانیاری سیستەم و بازاری عێراق.

---

# 🗺️ نەخشەی تەواوی Zoho ERP — بۆ سیستەمی Odoo-level

> **دۆخی سەرەتا:** ٦٥ لاپەڕە، ١٥٠+ endpoint، ٢٤+ collection — بنیاتنانی مامناوەند
> **ئامانجی کۆتایی:** ERP-ی تەواو بۆ شرکەتی مامناوەند و گەورەی عێراق
> **دەوروپێی کۆ:** ٢٢ Sprint × ٢ هەفتە = **~٤٤ هەفتە (١١ مانگ)**

---

## 📌 ڕیزبەندی بازاری عێراق (پێشەوایی)

```
🥇 فەیز ٣ — E-Invoicing  → قانوونیە، ئەگەر دواوەبکەوی شرکەتەکە بازار دەدەست بدات
🥈 فەیز ٤ — Excel Export → هەموو ئەکاونتنت داوای ئەمە دەکات
🥉 فەیز ١ — CRM          → ڕشتەی فرۆشتن، گەشەی بەکارهێنان
🏅 فەیز ٧ — WhatsApp     → عێراق = WhatsApp-first بازاری
```

---

## 🔵 فەیز ١ — CRM کامل
**دۆخی ئێستا:** تەنها contact management | **ئامانج:** Odoo CRM-level

---

### Sprint 1.1 — Lead & Pipeline Backend
**⏱ ٢ هەفتە | Effort: L | ئەیگێنت: `ERP CRM` + `زۆهۆ باکئێند`**

**پێشمەرجەکان:**
- Auth + RBAC بەردەستە ✅
- Contacts collection بەردەستە ✅

**Backend Tasks (١٤ endpoint):**
```
POST   /api/crm/leads                        ← create lead
GET    /api/crm/leads                        ← list + filter (stage, owner, date)
GET    /api/crm/leads/{id}                   ← detail
PUT    /api/crm/leads/{id}                   ← update
DELETE /api/crm/leads/{id}                   ← archive
POST   /api/crm/leads/{id}/convert           ← lead → opportunity

POST   /api/crm/opportunities                ← create opportunity
GET    /api/crm/opportunities                ← list with filters
GET    /api/crm/opportunities/{id}           ← detail
PUT    /api/crm/opportunities/{id}           ← update (stage, amount, probability)
DELETE /api/crm/opportunities/{id}           ← archive

GET    /api/crm/stages                       ← list pipeline stages
POST   /api/crm/stages                       ← create custom stage
PUT    /api/crm/stages/{id}                  ← rename / reorder
```

**Firestore Collections تازە:**
- `crm_leads` — `{org_id, name, contact_id, source, stage_id, owner_id, expected_revenue, probability, created_at}`
- `crm_opportunities` — `{org_id, lead_id, stage_id, amount, probability, close_date, owner_id}`
- `crm_stages` — `{org_id, name, sequence, is_won, is_lost, color}`

**Frontend Tasks:** — (backend sprint)

**Definition of Done:**
- [ ] Lead CRUD + convert to opportunity
- [ ] Stages custom-configurable
- [ ] All endpoints return `org_id`-scoped data
- [ ] test_all.py تێستی نوێ زیادکراوە

---

### Sprint 1.2 — Pipeline Kanban UI
**⏱ ٢ هەفتە | Effort: L | ئەیگێنت: `زۆهۆ فرۆنتئێند` + `ERP UX Designer`**

**پێشمەرجەکان:** Sprint 1.1 تەواو ✅

**Frontend Tasks (٦ لاپەڕە + ٧ کۆمپۆنێنت):**
```
📄 /crm/pipeline        ← Kanban board (drag-and-drop stage change)
📄 /crm/leads           ← Lead list (filter by stage, owner, source)
📄 /crm/leads/new       ← New lead form
📄 /crm/leads/{id}      ← Lead detail + timeline
📄 /crm/opportunities   ← Opportunity list
📄 /crm/opportunities/{id} ← Opportunity detail
```

**کۆمپۆنێنتەکان:**
```
🧩 KanbanBoard          ← @dnd-kit/sortable (column + card drag)
🧩 KanbanColumn         ← stage header + total value + cards
🧩 LeadCard             ← contact name, amount, probability badge
🧩 OpportunityForm      ← create/edit form
🧩 StageManager         ← CRUD for pipeline stages (modal)
🧩 ProbabilitySlider    ← visual 0-100%
🧩 ConvertLeadModal     ← lead → opportunity wizard
```

**i18n keys (ku.json + en.json):**
```json
"crm.pipeline", "crm.lead", "crm.opportunity", "crm.stage",
"crm.probability", "crm.expectedRevenue", "crm.closeDate",
"crm.source", "crm.won", "crm.lost", "crm.convertToOpp"
```

**Definition of Done:**
- [ ] Kanban board لوادبوونی کارد لانێوان stage ـەکان
- [ ] Lead → Opportunity convert flow
- [ ] فلتەر بەپێی stage + owner

---

### Sprint 1.3 — Activities + Forecasting
**⏱ ٢ هەفتە | Effort: M | ئەیگێنت: `ERP CRM` + `زۆهۆ فرۆنتئێند`**

**پێشمەرجەکان:** Sprint 1.2 تەواو ✅

**Backend Tasks (٩ endpoint):**
```
POST   /api/crm/activities                   ← schedule (call, email, meeting)
GET    /api/crm/activities                   ← list by lead_id or user
PUT    /api/crm/activities/{id}/done         ← mark completed
DELETE /api/crm/activities/{id}              ← cancel

GET    /api/crm/forecast                     ← monthly forecast (revenue × probability)
GET    /api/crm/reports/pipeline             ← pipeline value by stage
GET    /api/crm/reports/won-lost             ← win rate + average deal size
GET    /api/crm/reports/leaderboard          ← top salespeople
POST   /api/crm/activities/bulk-remind       ← send reminders for overdue
```

**Frontend Tasks (٢ لاپەڕە + ٤ کۆمپۆنێنت):**
```
📄 /crm/activities      ← Activity feed (today, upcoming, overdue)
📄 /crm/forecast        ← Forecasting page
```
```
🧩 ActivityTimeline     ← embedded in lead/opp detail (AntD Timeline)
🧩 ActivityForm         ← schedule activity modal
🧩 ForecastChart        ← bar chart monthly projection (Recharts)
🧩 WinLossWidget        ← donut chart win/loss ratio
```

**Definition of Done:**
- [ ] Activities (call/email/meeting) schedulable لە lead و opportunity
- [ ] Overdue activities لە dashboard دیارن
- [ ] Monthly forecast chart کاردەکات
- [ ] Win rate report

**📊 فەیز ١ کۆ:** ٢٣ endpoint تازە | ٨ لاپەڕە | ٣ collection تازە

---

## 🟢 فەیز ٢ — HR/Payroll UI کامل
**دۆخی ئێستا:** Service layer بووە، UI نییە | **ئامانج:** Full HR module

---

### Sprint 2.1 — Employee Management UI
**⏱ ٢ هەفتە | Effort: M | ئەیگێنت: `ERP HR + Payroll` + `زۆهۆ فرۆنتئێند`**

**پێشمەرجەکان:**
- HR backend service layer بەردەستە (gap check پێویستە)

**Backend Tasks (gap-fill، ٨ endpoint):**
```
GET    /api/hr/employees                     ← list + filter (dept, status)
POST   /api/hr/employees                     ← create
GET    /api/hr/employees/{id}                ← profile detail
PUT    /api/hr/employees/{id}                ← update
DELETE /api/hr/employees/{id}                ← archive

GET    /api/hr/departments                   ← list
POST   /api/hr/departments                   ← create
GET    /api/hr/positions                     ← job positions list
```

**Frontend Tasks (٤ لاپەڕە + ٥ کۆمپۆنێنت):**
```
📄 /hr/employees            ← Employee list (search, filter by dept)
📄 /hr/employees/new        ← New employee (personal info + contract + bank)
📄 /hr/employees/{id}       ← Employee profile (tabs: info, attendance, payslips)
📄 /hr/departments          ← Department CRUD
```
```
🧩 EmployeeCard             ← avatar + name + title + dept
🧩 EmployeeForm             ← 3-tab form (personal / contract / bank)
🧩 DepartmentTree           ← org chart (AntD Tree)
🧩 EmployeeStatusBadge      ← active / on-leave / terminated
🧩 ContractSummary          ← salary type + amount + dates
```

---

### Sprint 2.2 — Attendance + Leave Management
**⏱ ٢ هەفتە | Effort: M | ئەیگێنت: `ERP HR + Payroll`**

**پێشمەرجەکان:** Sprint 2.1 تەواو ✅

**Backend Tasks (٩ endpoint):**
```
POST   /api/hr/attendance/check-in           ← manual check-in
POST   /api/hr/attendance/check-out          ← manual check-out
GET    /api/hr/attendance                    ← list by employee + date range
GET    /api/hr/attendance/summary/{emp_id}   ← monthly hours summary

POST   /api/hr/leaves/request                ← request leave
PUT    /api/hr/leaves/{id}/approve           ← approve (manager)
PUT    /api/hr/leaves/{id}/reject            ← reject with reason
GET    /api/hr/leaves                        ← list (filter by status, employee)
GET    /api/hr/leaves/balance/{emp_id}       ← annual/sick leave balance
```

**Frontend Tasks (٣ لاپەڕە + ٤ کۆمپۆنێنت):**
```
📄 /hr/attendance       ← Attendance log + manual entry
📄 /hr/leaves           ← Leave request management (manager view)
📄 /hr/leaves/my        ← Employee's own leave requests
```
```
🧩 AttendanceTable      ← daily log (check-in, check-out, hours)
🧩 LeaveCalendar        ← AntD Calendar with leave markers
🧩 LeaveRequestForm     ← type + dates + reason modal
🧩 LeaveBalanceCard     ← remaining days per type
```

---

### Sprint 2.3 — Payroll UI
**⏱ ٢ هەفتە | Effort: L | ئەیگێنت: `ERP HR + Payroll` + `زۆهۆ ئەکاونتینگ`**

**پێشمەرجەکان:** Sprint 2.2 تەواو ✅

**Backend Tasks (٨ endpoint):**
```
POST   /api/hr/payroll/run                   ← run payroll (period + employees)
GET    /api/hr/payroll/slips                 ← list payslips (filter period, emp)
GET    /api/hr/payroll/slips/{id}            ← detail (lines breakdown)
PUT    /api/hr/payroll/slips/{id}/confirm    ← confirm payslip
PUT    /api/hr/payroll/slips/{id}/pay        ← mark paid + journal entry
GET    /api/hr/payroll/slips/{id}/pdf        ← PDF payslip (Arabic/Kurdish)
GET    /api/hr/payroll/report                ← monthly payroll cost summary
POST   /api/hr/payroll/batch-pay             ← bulk pay all confirmed slips
```

**Frontend Tasks (٣ لاپەڕە + ٤ کۆمپۆنێنت):**
```
📄 /hr/payroll              ← Payroll run page (select period)
📄 /hr/payroll/slips        ← Payslip list
📄 /hr/payroll/slips/{id}   ← Payslip detail + print
```
```
🧩 PayrollRunWizard         ← 3-step (select → preview → confirm)
🧩 PayslipBreakdown         ← earnings / deductions / net table
🧩 BatchPayButton           ← bulk payment + journal creation
🧩 PayrollSummaryChart      ← monthly payroll cost trend
```

**Definition of Done فەیز ٢:**
- [ ] Full employee CRUD + departments + org chart
- [ ] Attendance manual entry + monthly summary
- [ ] Leave request → approve/reject → balance deduction
- [ ] Payroll run → payslips → PDF → journal entry
- [ ] IQD salary + withholding tax calculation

**📊 فەیز ٢ کۆ:** ٢٥ endpoint تازە | ١٠ لاپەڕە | ٣ collection تازە

---

## 🟡 فەیز ٣ — Iraq E-Invoicing Integration
**دۆخی ئێستا:** Hooks بووە، تەواو نییە | **ئامانج:** Full ITA compliance
> ⚠️ **ئەم فەیزە ئەوەڵترین پێشەوایی بۆ بازاری عێراق — قانوونی فیدرالی**

---

### Sprint 3.1 — E-Invoice Core Engine
**⏱ ٢ هەفتە | Effort: XL | ئەیگێنت: `ERP Localization Iraq` + `زۆهۆ باکئێند`**

**پێشمەرجەکان:**
- Invoice module بەردەستە ✅
- ITA portal credentials (بەکارهێنەر پێویستە بیبات)

**Backend Tasks (٨ endpoint):**
```
POST   /api/einvoice/submit/{invoice_id}     ← sign + submit to ITA
GET    /api/einvoice/status/{invoice_id}     ← check submission status
POST   /api/einvoice/cancel/{invoice_id}     ← cancellation request
GET    /api/einvoice/config                  ← get ITA config
PUT    /api/einvoice/config                  ← update credentials

GET    /api/einvoice/qr/{invoice_id}         ← generate QR code (Base64)
POST   /api/einvoice/xml/{invoice_id}        ← generate XML (Iraqi ETA standard)
POST   /api/einvoice/sign/{invoice_id}       ← digital signature
```

**سێرڤیسە تازەکان (backend):**
```python
# services/einvoice_service.py
- generate_xml(invoice)     ← Iraqi ETA XML schema
- sign_xml(xml, cert)       ← RSA digital signature
- generate_qr(invoice)      ← TLV encoding per ITA standard
- submit_to_ita(signed_xml) ← HTTP POST to ITA portal
- verify_response(response) ← parse ITA UUID response
```

**Frontend Tasks (٢ لاپەڕە + ٤ کۆمپۆنێنت):**
```
📄 /settings/einvoice       ← ITA credentials + portal URL settings
📄 /einvoice/dashboard      ← compliance overview
```
```
🧩 EInvoiceStatusBadge      ← pending / submitted / accepted / rejected
🧩 SubmitToITAButton        ← on invoice detail page
🧩 QRCodeDisplay            ← show QR on invoice print
🧩 ITAConfigForm            ← credentials form (masked fields)
```

---

### Sprint 3.2 — Compliance Reports + Automation
**⏱ ٢ هەفتە | Effort: M | ئەیگێنت: `ERP Localization Iraq`**

**پێشمەرجەکان:** Sprint 3.1 تەواو ✅

**Backend Tasks (٥ endpoint):**
```
GET    /api/einvoice/report/monthly          ← monthly submission summary
GET    /api/einvoice/report/errors           ← failed submissions + reason
POST   /api/einvoice/retry/{invoice_id}      ← retry failed submission
POST   /api/einvoice/auto-submit/toggle      ← enable auto-submit on confirm
POST   /api/einvoice/webhook                 ← ITA callback receiver
```

**Frontend Tasks (١ لاپەڕە + ٣ کۆمپۆنێنت):**
```
📄 /einvoice/reports        ← monthly compliance report
```
```
🧩 ComplianceSummaryCards   ← submitted / pending / failed / accepted
🧩 SubmissionLogTable       ← per-invoice status log
🧩 ErrorRetryPanel          ← list errors + retry button
```

**Definition of Done فەیز ٣:**
- [ ] XML generation بەپێی Iraqi ETA standard
- [ ] QR code لەسەر هەموو invoice PDF
- [ ] Submit → ITA UUID دەستکەوت → status tracking
- [ ] Cancel + Retry flow کاردەکات
- [ ] Monthly compliance report
- [ ] Credentials encrypted (Firestore، نا code)

**📊 فەیز ٣ کۆ:** ١٣ endpoint تازە | ٣ لاپەڕە | ١ collection تازە (`einvoice_submissions`)

---

## 🟠 فەیز ٤ — Excel/CSV Export + Advanced Reports
**دۆخی ئێستا:** تەنها PDF | **ئامانج:** Excel export + drill-down analytics

---

### Sprint 4.1 — Excel Export Engine
**⏱ ٢ هەفتە | Effort: M | ئەیگێنت: `زۆهۆ باکئێند` + `زۆهۆ فرۆنتئێند`**

**پێشمەرجەکان:** هیچ dependency نییە ✅

**Backend Tasks (٩ endpoint):**
```
GET    /api/export/invoices                  ← ?format=excel|csv&date_from&date_to
GET    /api/export/bills                     ← bills Excel
GET    /api/export/journal-entries           ← journal entries
GET    /api/export/trial-balance             ← trial balance Excel
GET    /api/export/customers                 ← customer list
GET    /api/export/products                  ← product catalog
GET    /api/export/pos-sales                 ← POS sales
GET    /api/export/inventory                 ← stock levels
GET    /api/export/payroll                   ← payroll report Excel
```

**`ExportService` (backend/app/services/export_service.py):**
```python
class ExportService:
    def to_excel(data, columns, title, rtl=True) → BytesIO  # openpyxl
    def to_csv(data, columns) → str
    # Kurdish RTL headers + IQD formatting
    # Auto column width + header styling
```

**Frontend Tasks (١ کۆمپۆنێنت، زیادکردن بۆ هەموو لاپەڕەکان):**
```
🧩 ExportButton             ← Dropdown: PDF / Excel / CSV
    ← زیادکردن بۆ: invoices, bills, journals, products,
       customers, POS reports, inventory, payroll
```

---

### Sprint 4.2 — Advanced Financial Reports
**⏱ ٢ هەفتە | Effort: L | ئەیگێنت: `زۆهۆ ئەکاونتینگ` + `زۆهۆ فرۆنتئێند`**

**پێشمەرجەکان:** Sprint 4.1 بۆ Excel export ✅

**Backend Tasks (٨ endpoint):**
```
GET    /api/reports/cash-flow                ← cash flow statement (indirect method)
GET    /api/reports/profit-loss/detailed     ← P&L with drill-down to accounts
GET    /api/reports/balance-sheet/detailed   ← detailed balance sheet
GET    /api/reports/aging/receivables        ← AR aging (30/60/90/120+ days)
GET    /api/reports/aging/payables           ← AP aging
GET    /api/reports/sales/by-product         ← top products by revenue
GET    /api/reports/sales/by-customer        ← top customers
GET    /api/reports/tax/summary              ← VAT + withholding monthly summary
```

**Frontend Tasks (٤ لاپەڕە + ٣ کۆمپۆنێنت):**
```
📄 /reports/cash-flow       ← Cash flow statement + Excel
📄 /reports/aging           ← AR/AP aging tabs
📄 /reports/sales-analytics ← product/customer analytics
📄 /reports/tax-summary     ← VAT compliance report
```
```
🧩 DrillDownTable           ← expandable rows (AntD Table expandable)
🧩 AgingBucketChart         ← stacked bar (30/60/90/120+ days)
🧩 SalesHeatmap             ← monthly × product/customer matrix
```

**Definition of Done فەیز ٤:**
- [ ] هەموو لیست-لاپەڕەکان Excel + CSV export دەکەن
- [ ] Cash flow statement generated (indirect method)
- [ ] AR/AP aging بە bucket ـەکان
- [ ] Sales analytics by product + customer
- [ ] Tax summary report بۆ ITA compliance

**📊 فەیز ٤ کۆ:** ١٧ endpoint تازە | ٥ لاپەڕە تازە | ١ component global

---

## 🔷 فەیز ٥ — UX Upgrade
**دۆخی ئێستا:** Standard AntD light | **ئامانج:** Dark mode + Inter + drill-down dashboard

---

### Sprint 5.1 — Dark Mode + Typography
**⏱ ٢ هەفتە | Effort: M | ئەیگێنت: `ERP UX Designer` + `زۆهۆ فرۆنتئێند`**

**پێشمەرجەکان:** هیچ dependency نییە ✅

**Frontend Tasks:**
```
🔧 ThemeProvider.tsx        ← AntD ConfigProvider dark/light token
🔧 useTheme.ts              ← Zustand store (theme: 'light'|'dark', persist)
🔧 DarkModeToggle.tsx       ← header button (sun/moon icon)
🔧 global.css               ← CSS custom properties for theme vars
🔧 Inter font               ← Google Fonts self-hosted (performance)
🔧 Typography scale         ← h1-h6 + body + caption tokens
```

**AntD Dark Token Override:**
```typescript
// تەنها token-based override — نا CSS hack
theme: {
  algorithm: isDark ? theme.darkAlgorithm : theme.defaultAlgorithm,
  token: { fontFamily: 'Inter, sans-serif', ... }
}
```

**Backend Tasks (٢ endpoint):**
```
GET    /api/user/preferences                 ← theme + language per user
PUT    /api/user/preferences                 ← save preference to Firestore
```

---

### Sprint 5.2 — Dashboard Drill-Down
**⏱ ٢ هەفتە | Effort: L | ئەیگێنت: `ERP UX Designer` + `زۆهۆ فرۆنتئێند`**

**پێشمەرجەکان:** Sprint 5.1 ✅، Phase 4 reports endpoint ✅

**Backend Tasks (٣ endpoint):**
```
GET    /api/dashboard/kpis                   ← revenue, expenses, profit, cash (1 call)
GET    /api/dashboard/charts/sales           ← timeseries (period: day|week|month|year)
GET    /api/dashboard/alerts                 ← overdue invoices, low stock, activities
```

**Frontend Tasks (لاپەڕەی `/dashboard` دیزاینی نوێ):**
```
🧩 KPICard                  ← click → drill-down modal (AntD Statistic + Trend)
🧩 SalesChart               ← period toggle: daily/weekly/monthly (Recharts Area)
🧩 TopCustomersWidget       ← top 5 by revenue (Progress bars)
🧩 TopProductsWidget        ← top 5 by qty (horizontal bar)
🧩 CashFlowMiniChart        ← sparkline (in/out 30 days)
🧩 AlertsPanel              ← overdue, low stock, upcoming activities
🧩 RecentTransactionsFeed   ← last 10 invoices + bills
```

**Definition of Done فەیز ٥:**
- [ ] Dark mode لە هەموو ٦٥+ لاپەڕەدا کاردەکات
- [ ] Inter font applied globally
- [ ] Theme preference save ئەبێت
- [ ] Dashboard ٦+ KPI card بە drill-down
- [ ] Charts interactive (hover tooltip + period toggle)

**📊 فەیز ٥ کۆ:** ٥ endpoint تازە | ٠ لاپەڕەی نوێ (redesign) | تەواوی UX shift

---

## 🟣 فەیز ٦ — MRP کامل
**دۆخی ئێستا:** Skeleton | **ئامانج:** Bill of Materials + Work Orders

---

### Sprint 6.1 — Bill of Materials
**⏱ ٢ هەفتە | Effort: M | ئەیگێنت: `ERP Inventory + MRP`**

**پێشمەرجەکان:**
- Inventory + Products collection بەردەستن ✅

**Backend Tasks (٩ endpoint):**
```
POST   /api/mrp/bom                          ← create BOM
GET    /api/mrp/bom                          ← list BOMs (filter by product)
GET    /api/mrp/bom/{id}                     ← detail with components
PUT    /api/mrp/bom/{id}                     ← update
DELETE /api/mrp/bom/{id}                     ← archive

POST   /api/mrp/bom/{id}/components          ← add component line
PUT    /api/mrp/bom/{id}/components/{cid}    ← update qty/uom
DELETE /api/mrp/bom/{id}/components/{cid}    ← remove component

GET    /api/mrp/bom/{id}/cost                ← compute BOM cost
```

**Firestore Collections:** `mrp_bom`, `mrp_bom_lines`

**Frontend Tasks (٢ لاپەڕە + ٣ کۆمپۆنێنت):**
```
📄 /mrp/bom             ← BOM list
📄 /mrp/bom/new         ← Create BOM + dynamic components
```
```
🧩 BOMComponentTable    ← editable rows (product + qty + UOM)
🧩 BOMCostSummary       ← total material cost
🧩 NestedBOMBadge       ← indicator if component has own BOM
```

---

### Sprint 6.2 — Manufacturing Orders + Work Orders
**⏱ ٢ هەفتە | Effort: L | ئەیگێنت: `ERP Inventory + MRP`**

**پێشمەرجەکان:** Sprint 6.1 ✅

**Backend Tasks (١٢ endpoint):**
```
POST   /api/mrp/orders                       ← create manufacturing order (MO)
GET    /api/mrp/orders                       ← list MOs (status filter)
GET    /api/mrp/orders/{id}                  ← detail
PUT    /api/mrp/orders/{id}/confirm          ← confirm + reserve materials
PUT    /api/mrp/orders/{id}/produce          ← record production qty
PUT    /api/mrp/orders/{id}/done             ← mark done → auto stock move (BOM → finished)

GET    /api/mrp/orders/{id}/workorders       ← list work orders
POST   /api/mrp/orders/{id}/workorders       ← add work order
PUT    /api/mrp/workorders/{id}/start        ← start (log start time)
PUT    /api/mrp/workorders/{id}/done         ← finish (log end time, duration)

GET    /api/mrp/workcenters                  ← list work centers
POST   /api/mrp/workcenters                  ← create
```

**Frontend Tasks (٣ لاپەڕە + ٣ کۆمپۆنێنت):**
```
📄 /mrp/orders          ← Manufacturing order list (kanban: draft/confirmed/in-progress/done)
📄 /mrp/orders/new      ← Create MO (select BOM + qty + deadline)
📄 /mrp/workcenters     ← Work center management
```
```
🧩 MOStatusKanban       ← manufacturing order pipeline
🧩 WorkOrderTimeline    ← AntD Steps (operations sequence)
🧩 ProductionProgress   ← qty produced vs planned (Progress bar)
```

**Definition of Done فەیز ٦:**
- [ ] BOM create/edit با nested component support
- [ ] MO → confirm → produce → done → stock auto-update
- [ ] Work orders بە start/done timestamps
- [ ] BOM cost computation

**📊 فەیز ٦ کۆ:** ٢١ endpoint تازە | ٥ لاپەڕە | ٢ collection تازە

---

## 🔴 فەیز ٧ — WhatsApp + OCR Integration
**دۆخی ئێستا:** هیچ | **ئامانج:** Meta Cloud API + Google Vision OCR

---

### Sprint 7.1 — WhatsApp Notifications
**⏱ ٢ هەفتە | Effort: L | ئەیگێنت: `ERP Integration`**

**پێشمەرجەکان:**
- Meta Business Account + WhatsApp Cloud API access (بەکارهێنەر پێویستە)

**Backend Tasks (٨ endpoint):**
```
PUT    /api/settings/whatsapp                ← save API credentials (encrypted)
GET    /api/whatsapp/templates               ← list approved message templates
POST   /api/whatsapp/templates               ← create template (pending Meta approval)
POST   /api/whatsapp/send                    ← generic send (template + variables)
POST   /api/whatsapp/invoice/{id}/send       ← send invoice link to customer
POST   /api/whatsapp/reminder/{customer_id}  ← overdue payment reminder
POST   /api/whatsapp/webhook                 ← receive incoming messages (Meta verify + messages)
GET    /api/whatsapp/log                     ← sent messages history
```

**`WhatsAppService` (backend/app/services/whatsapp_service.py):**
```python
# Meta Cloud API v18+
- send_template(to, template_name, variables) → message_id
- send_text(to, text) → message_id
- verify_webhook(token, challenge) → challenge
# credentials from Firestore (org-scoped, AES-encrypted)
```

**Frontend Tasks (٢ لاپەڕە + ٣ کۆمپۆنێنت):**
```
📄 /settings/whatsapp       ← WhatsApp config (phone number, API key, webhook)
📄 /whatsapp/log            ← message history
```
```
🧩 SendWhatsAppButton       ← on invoice detail (send to customer number)
🧩 WhatsAppTemplateSelect   ← dropdown template chooser
🧩 WhatsAppLogTable         ← status: sent / delivered / read / failed
```

---

### Sprint 7.2 — OCR Receipt Scanning
**⏱ ٢ هەفتە | Effort: L | ئەیگێنت: `ERP Integration` + `زۆهۆ باکئێند`**

**پێشمەرجەکان:** Sprint 7.1 (independent — parallel possible) ✅

**Backend Tasks (٤ endpoint):**
```
POST   /api/ocr/scan                         ← upload image → extract (Google Vision)
POST   /api/ocr/scan/bill                    ← scan → create bill draft
POST   /api/ocr/scan/receipt                 ← scan → create expense draft
GET    /api/ocr/history                      ← past scans + status
```

**`OCRService` (backend/app/services/ocr_service.py):**
```python
# Google Cloud Vision API (Document Text Detection)
- extract_text(image_bytes) → raw_text
- parse_invoice_data(text) → {vendor, date, total, tax, items}
- create_bill_draft(parsed_data, org_id) → bill_id
# Fallback: pytesseract (offline) if Vision API unavailable
```

**Frontend Tasks (٣ کۆمپۆنێنت):**
```
🧩 OCRScanButton            ← drag-drop + camera capture (on bills/expenses page)
🧩 OCRResultPreview         ← show extracted fields (editable before save)
🧩 OCRUploadModal           ← step 1: upload → step 2: review → step 3: confirm
```

**Definition of Done فەیز ٧:**
- [ ] WhatsApp invoice send بۆ customer number کاردەکات
- [ ] Payment reminders sendable (template-based)
- [ ] Webhook verified + incoming messages logged
- [ ] OCR extract vendor + amount + date from receipt photo
- [ ] OCR creates bill draft بۆ review
- [ ] All credentials encrypted لە Firestore

**📊 فەیز ٧ کۆ:** ١٢ endpoint تازە | ٢ لاپەڕە | ٢ collection تازە

---

## 🟤 فەیز ٨ — Mobile PWA
**دۆخی ئێستا:** Desktop only | **ئامانج:** Installable + Offline-capable PWA

---

### Sprint 8.1 — PWA Foundation
**⏱ ٢ هەفتە | Effort: M | ئەیگێنت: `ERP UX Designer` + `شادۆ ئەدا`**

**پێشمەرجەکان:** هیچ ✅

**Frontend Tasks:**
```
📦 vite-plugin-pwa         ← Workbox service worker
📄 public/manifest.json    ← name: "Zoho ERP", icons (192/512), theme_color
🔧 Service Worker Strategy ← NetworkFirst (API), CacheFirst (assets)
🔧 Offline page            ← /offline.html
🔧 MobileNavBar.tsx        ← bottom navigation (5 tabs: dashboard/sales/pos/crm/more)
🔧 Responsive Audit        ← بپشکنە هەموو ٦٥+ لاپەڕە بۆ mobile breakpoint
🔧 InstallPrompt.tsx       ← "نصب بکە" banner (beforeinstallprompt)
```

**Backend Tasks (٢ endpoint):**
```
POST   /api/notifications/subscribe          ← register FCM token
DELETE /api/notifications/unsubscribe        ← remove FCM token
```

---

### Sprint 8.2 — Mobile-Optimized Views
**⏱ ٢ هەفتە | Effort: L | ئەیگێنت: `ERP UX Designer`**

**پێشمەرجەکان:** Sprint 8.1 ✅

**Backend Tasks (١ endpoint):**
```
GET    /api/mobile/dashboard                 ← lightweight KPIs (min data, fast)
```

**Frontend Tasks (٤ لاپەڕە + ٤ کۆمپۆنێنت):**
```
📄 /m/dashboard             ← mobile dashboard (2-column KPI grid)
📄 /m/pos                   ← mobile POS (optimized for tablet/phone)
📄 /m/invoices              ← swipe-to-action invoice list
📄 /m/quick-expense         ← single-screen expense entry
```
```
🧩 SwipeableListItem        ← swipe left: edit, right: delete (confirm)
🧩 BottomSheet              ← mobile-native feel modals
🧩 QuickActionFAB           ← floating action button (+ new invoice/bill/expense)
🧩 MobileKPIGrid            ← 2×2 grid with sparklines
```

**Definition of Done فەیز ٨:**
- [ ] Installable as PWA لە Android + iOS (Add to Home Screen)
- [ ] Offline: last 100 transactions cached and readable
- [ ] Mobile POS قابلی بەکارهێنان لە tablet
- [ ] Push notification دەگات کاتێک invoice پارە دراوە
- [ ] Core Web Vitals: LCP < 2.5s، CLS < 0.1

**📊 فەیز ٨ کۆ:** ٣ endpoint تازە | ٤ لاپەڕەی mobile-optimized

---

## ⚫ فەیز ٩ — Multi-Company + Multi-Branch
**دۆخی ئێستا:** Single org only | **ئامانج:** Consolidated group + branch management

---

### Sprint 9.1 — Multi-Company Foundation
**⏱ ٢ هەفتە | Effort: XL | ئەیگێنت: `زۆهۆ داتابەیس` + `زۆهۆ باکئێند`**

**پێشمەرجەکان:**
- هەموو فەیزە پێشوەکان تەواون ✅
- ئەمە مۆدیوڵی گەورەترینە — `company_id` scoping لە سەرووی `org_id`

**Backend Tasks (٩ endpoint):**
```
POST   /api/companies                        ← create subsidiary (parent_org_id)
GET    /api/companies                        ← list user's companies
GET    /api/companies/{id}                   ← detail
PUT    /api/companies/{id}                   ← update settings
POST   /api/companies/{id}/switch            ← switch active company (update JWT context)
DELETE /api/companies/{id}                   ← archive subsidiary

GET    /api/companies/consolidated/pl        ← consolidated P&L
GET    /api/companies/consolidated/bs        ← consolidated balance sheet
POST   /api/companies/intercompany           ← inter-company journal entry
```

**Data Isolation Strategy:**
```python
# هەموو repository ـەکان company_id زیاد دەکەن
# company_id لە JWT payload (sub-scope of org_id)
# Existing org_id → maps to default company
# New subsidiaries → own company_id under parent org_id
```

**Frontend Tasks (٢ لاپەڕە + ٢ کۆمپۆنێنت):**
```
📄 /settings/companies      ← Company list + create subsidiary
📄 /reports/consolidated    ← Consolidated financial reports
```
```
🧩 CompanySwitcher          ← header dropdown (logo + name)
🧩 CompanyBadge             ← shows active company on all pages
```

---

### Sprint 9.2 — Branch Management
**⏱ ٢ هەفتە | Effort: M | ئەیگێنت: `زۆهۆ باکئێند`**

**پێشمەرجەکان:** Sprint 9.1 ✅

**Backend Tasks (٨ endpoint):**
```
POST   /api/branches                         ← create branch
GET    /api/branches                         ← list branches (of active company)
GET    /api/branches/{id}                    ← detail
PUT    /api/branches/{id}                    ← update (name, address, phone)
DELETE /api/branches/{id}                    ← archive

PUT    /api/users/{id}/branch                ← assign user to branch
GET    /api/reports/branches/comparison      ← revenue/expense by branch (bar chart)
PUT    /api/pos/terminals/{id}/branch        ← assign POS terminal to branch
```

**Frontend Tasks (٢ لاپەڕە + ٢ کۆمپۆنێنت):**
```
📄 /settings/branches       ← Branch CRUD
📄 /reports/branches        ← Branch comparison report
```
```
🧩 BranchSelector           ← filter on all reports
🧩 BranchComparisonChart    ← grouped bar chart (revenue by branch)
```

**Definition of Done فەیز ٩:**
- [ ] 2+ companies create/switch کاردەکات
- [ ] Data تەواو isolated بە company_id
- [ ] Consolidated P&L + Balance Sheet
- [ ] Branch CRUD + user assignment
- [ ] POS terminal → branch linked
- [ ] Branch comparison report

**📊 فەیز ٩ کۆ:** ١٧ endpoint تازە | ٤ لاپەڕە تازە

---

## ⚪ فەیز ١٠ — Production Deployment + DevOps
**دۆخی ئێستا:** Dev environment | **ئامانج:** Production-ready CI/CD + monitoring

---

### Sprint 10.1 — Docker + CI/CD
**⏱ ٢ هەفتە | Effort: L | ئەیگێنت: `ERP DevOps` + `شادۆ DevOps`**

**پێشمەرجەکان:** هەموو فەیزی پێشوو ✅

**DevOps Tasks:**
```dockerfile
# backend/Dockerfile  (multi-stage: builder + runtime)
FROM python:3.11-slim AS builder
# ... pip install + compile

# frontend/Dockerfile  (Node build + Nginx serve)
FROM node:20-alpine AS builder
# ... npm run build
FROM nginx:alpine
```

```yaml
# docker-compose.prod.yml
services:
  backend:   image: zoho-backend:latest, port: 8000
  frontend:  image: zoho-frontend:latest, port: 80
  nginx:     reverse proxy (SSL termination)
```

**GitHub Actions:**
```yaml
# .github/workflows/ci.yml  — on: pull_request
- pytest backend tests
- npm run build frontend
- تێستی TypeScript strict

# .github/workflows/deploy.yml  — on: push to main
- Build Docker images
- Push to registry
- Deploy to VPS (SSH)
```

**Nginx Config:** SSL (Let's Encrypt) + gzip + cache headers + rate limiting

---

### Sprint 10.2 — Monitoring + Security Hardening
**⏱ ٢ هەفتە | Effort: M | ئەیگێنت: `ERP DevOps` + `ERP Security + Audit`**

**پێشمەرجەکان:** Sprint 10.1 ✅

**Backend Tasks (٤ endpoint):**
```
GET    /api/health                           ← basic: 200 OK + version
GET    /api/health/detailed                  ← Firestore ping + Auth status + response times
POST   /api/admin/backup                     ← manual Firestore export trigger
GET    /api/docs                             ← FastAPI auto-docs (Swagger UI + ReDoc)
```

**DevOps + Security Tasks:**
```
✅ Sentry SDK (backend + frontend)           ← error tracking
✅ Rate limiting (slowapi)                   ← per org_id: 1000 req/min
✅ Firebase Security Rules review            ← شادۆ ئاژێنت‌شیلد
✅ GCP Cloud Scheduler                       ← daily Firestore backup (GCS bucket)
✅ .env.example                             ← document all required env vars
✅ API documentation                         ← /docs (FastAPI auto) + README.md
✅ CORS hardening                            ← only production domain
✅ HTTP security headers                     ← HSTS, X-Frame-Options, CSP
```

**Definition of Done فەیز ١٠:**
- [ ] `docker compose up` تەواو سیستەم دەستپێدەکات
- [ ] CI/CD: PR → test → merge → auto-deploy
- [ ] Health endpoint 200 OK
- [ ] Sentry errors capture دەکات
- [ ] Daily backup scheduled
- [ ] API docs at `/docs`
- [ ] SSL + security headers
- [ ] Rate limiting کاردەکات

**📊 فەیز ١٠ کۆ:** ٤ endpoint تازە | ٠ لاپەڕەی نوێ | infrastructure تەواو

---

## 📊 ئامارەی گشتی

```
┌─────────────────────────────────────────────────────────────────┐
│                    نەخشەی تەواوی ERP                           │
├────────────────┬──────────┬──────────┬────────────┬────────────┤
│ فەیز           │ Sprint   │ هەفتە    │ Endpoint   │ لاپەڕە    │
├────────────────┼──────────┼──────────┼────────────┼────────────┤
│ ١ CRM          │ 3        │ 6        │ +23        │ +8         │
│ ٢ HR/Payroll   │ 3        │ 6        │ +25        │ +10        │
│ ٣ E-Invoice    │ 2        │ 4        │ +13        │ +3         │
│ ٤ Excel/Report │ 2        │ 4        │ +17        │ +5         │
│ ٥ UX Upgrade   │ 2        │ 4        │ +5         │ 0 (redesign)│
│ ٦ MRP          │ 2        │ 4        │ +21        │ +5         │
│ ٧ WhatsApp+OCR │ 2        │ 4        │ +12        │ +2         │
│ ٨ Mobile PWA   │ 2        │ 4        │ +3         │ +4         │
│ ٩ Multi-company│ 2        │ 4        │ +17        │ +4         │
│ ١٠ DevOps      │ 2        │ 4        │ +4         │ 0          │
├────────────────┼──────────┼──────────┼────────────┼────────────┤
│ 🆕 زیادبووە    │ 22       │ 44       │ +140       │ +41        │
│ ✅ بەردەستە   │ —        │ —        │ 150+       │ 65         │
│ 🎯 کۆ          │ 22       │ 44       │ ~290+      │ ~106       │
└────────────────┴──────────┴──────────┴────────────┴────────────┘
```

---

## 🗓️ Timeline نەخشە (بەپێی ئێستا — April 2026)

```
Apr 2026  ████ S1.1 CRM Backend
May 2026  ████ S1.2 CRM Kanban UI  |  S1.3 Activities
Jun 2026  ████ S2.1 Employee UI    |  S2.2 Attendance
Jul 2026  ████ S2.3 Payroll        |  S3.1 E-Invoice Core ⚡
Aug 2026  ████ S3.2 Compliance     |  S4.1 Excel Export
Sep 2026  ████ S4.2 Reports        |  S5.1 Dark Mode
Oct 2026  ████ S5.2 Dashboard      |  S6.1 BOM
Nov 2026  ████ S6.2 MFG Orders     |  S7.1 WhatsApp
Dec 2026  ████ S7.2 OCR            |  S8.1 PWA
Jan 2027  ████ S8.2 Mobile Views   |  S9.1 Multi-Company
Feb 2027  ████ S9.2 Branches       |  S10.1 Docker CI/CD
Mar 2027  ████ S10.2 Monitor+Security → 🚀 Production v2.0
```

---

## 🏁 ئەیگێنتەکان بۆ هەر فەیز

| فەیز | ئەیگێنتە سەرەکییەکان | سکیڵ‌ پێویست |
|------|----------------------|--------------|
| ١ CRM | `ERP CRM` + `زۆهۆ فرۆنتئێند` | `react19-patterns`, `firestore-patterns` |
| ٢ HR | `ERP HR + Payroll` + `زۆهۆ ئەکاونتینگ` | `python-patterns`, `antd-rtl-patterns` |
| ٣ E-Invoice | `ERP Localization Iraq` | `security-review-owasp` |
| ٤ Reports | `زۆهۆ ئەکاونتینگ` + `زۆهۆ فرۆنتئێند` | `api-design-fastapi` |
| ٥ UX | `ERP UX Designer` + `شادۆ ئەدا` | `antd-rtl-patterns`, `typescript-strict` |
| ٦ MRP | `ERP Inventory + MRP` | `firestore-patterns` |
| ٧ Integrations | `ERP Integration` | `security-review-owasp` |
| ٨ PWA | `ERP UX Designer` + `شادۆ ئەدا` | `react19-patterns` |
| ٩ Multi-company | `زۆهۆ داتابەیس` + `زۆهۆ باکئێند` | `firestore-patterns` |
| ١٠ DevOps | `ERP DevOps` + `ERP Security + Audit` | `deployment-windows`, `agentshield-rules` |

---

> **ئامادەی دەوروپێی:** وا بڵێین کە ١ دەڤەلۆپەر Sprint ـێک بەم ڕاپۆرتە جێبەجێ دەکات — ئەگەر تیمی ٢ کەس: وابڕدار نزیکەی ٦ مانگ. ئەگەر ١ کەس: ١١ مانگ ئامانجێکی واقەعییە.