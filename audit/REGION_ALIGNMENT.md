# Region alignment audit

Generated by `tools/verify_region_alignment.py`.

| Component | Expected | Actual | Status |
|-----------|----------|--------|--------|
| Vercel /api proxy -> Cloud Run | europe-west1 | https://zoho-erp-backend-6plfqh2hiq-ew.a.run.app/api/:path* | OK |
| Cloud Run region | europe-west1 | europe-west1 | OK |
| Firestore | eur3 | eur3 | OK |

## Notes

- Firestore `eur3` and Cloud Run `europe-west1` are the correct EU-latency pair.
- Hosting is on **Vercel** (SPA + `/api/*` proxy to Cloud Run); `firebase.json` is Firestore-only by design — there is no Firebase Hosting block to check.
- All production resources live in one project: `zoho-83cda`.
