# Task Dependencies Repository
from .base import BaseRepository

class TaskDependencyRepository(BaseRepository):
    """Repository for task dependencies (project Gantt)"""
    collection_name = "task_dependencies"
